import 'package:flutter/material.dart';

class ColorsManager {
  // اللون الأساسي (بنفسجي قوي)
  static const Color primary = Color(0xFF771BDA);

  // لون ثانوي فاتح (خلفيات/بطاقات)
  static const Color secondary = Color(0xFFF3E8FF);

  // لون Accent - أصفر مشمشي (للأزرار المهمة أو التنبيهات)
  static const Color accent = Color(0xFFFFD54F);

  // لون الخلفية العامة للتطبيق
  static const Color background = Color(0xFFFDFDFD); // تقريبًا أبيض لكن مريح

  // لون للبطاقات الفاتحة جدًا
  static const Color card = Color(0xFFF9F6FD);

  // لون النص الرئيسي (غامق)
  static const Color textPrimary = Color(0xFF212121);

  // لون النص الثانوي / التوضيحي
  static const Color textSecondary = Color(0xFF757575);

  // أبيض صافٍ (لأزرار فوق ألوان قوية)
  static const Color white = Color(0xFFFFFFFF);

  // أسود (لو احتجته مثلاً للنصوص المهمة جدًا)
  static const Color black = Color(0xFF000000);

  // لون أحمر خفيف (للتنبيهات أو الخطأ)
  static const Color error = Color(0xFFFF6B6B);

  // لون أخضر خفيف (للنجاح أو التأكيد)
  static const Color success = Color(0xFF4CAF50);

  // ظل ناعم للعناصر
  static const Color shadow = Color(0x1A000000); // 10% Black
}
